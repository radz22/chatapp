{
  "name": "deploy-try",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node backend/server.js",
    "server": "nodemon backend/server.js",
    "build": "npm-run-all install-deps build:frontend build:backend",
    "install-deps": "npm install --legacy-peer-deps && npm install --legacy-peer-deps --prefix frontend",
    "build:frontend": "cd frontend && vite build",
    "build:backend": "cd backend && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "gitignore": "^0.7.0",
    "nodemon": "^3.1.0",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
